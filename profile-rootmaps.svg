<?xml version="1.0" standalone="no"?><!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd"><svg version="1.1" width="1200" height="634" onload="init(evt)" viewBox="0 0 1200 634" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink"><!--Flame graph stack visualization. See https://github.com/brendangregg/FlameGraph for latest version, and http://www.brendangregg.com/flamegraphs.html for examples.--><!--NOTES: --><defs><linearGradient id="background" y1="0" y2="1" x1="0" x2="0"><stop stop-color="#eeeeee" offset="5%"/><stop stop-color="#eeeeb0" offset="95%"/></linearGradient></defs><style type="text/css">
text { font-family:"Verdana"; font-size:12px; fill:rgb(0,0,0); }
#title { text-anchor:middle; font-size:17px; }
#search { opacity:0.1; cursor:pointer; }
#search:hover, #search.show { opacity:1; }
#subtitle { text-anchor:middle; font-color:rgb(160,160,160); }
#unzoom { cursor:pointer; }
#frames > *:hover { stroke:black; stroke-width:0.5; cursor:pointer; }
.hide { display:none; }
.parent { opacity:0.5; }
</style><script type="text/ecmascript"><![CDATA[var nametype = 'Function:';
var fontsize = 12;
var fontwidth = 0.59;
var xpad = 10;
var inverted = true;
var searchcolor = 'rgb(230,0,230)';
var fluiddrawing = true;
var truncate_text_right = false;]]><![CDATA["use strict";
var details, searchbtn, unzoombtn, matchedtxt, svg, searching, frames;
function init(evt) {
    details = document.getElementById("details").firstChild;
    searchbtn = document.getElementById("search");
    unzoombtn = document.getElementById("unzoom");
    matchedtxt = document.getElementById("matched");
    svg = document.getElementsByTagName("svg")[0];
    frames = document.getElementById("frames");
    searching = 0;

    // Use GET parameters to restore a flamegraph's state.
    var restore_state = function() {
        var params = get_params();
        if (params.x && params.y)
            zoom(find_group(document.querySelector('[x="' + params.x + '"][y="' + params.y + '"]')));
        if (params.s)
            search(params.s);
    };

    if (fluiddrawing) {
        // Make width dynamic so the SVG fits its parent's width.
        svg.removeAttribute("width");
        // Edge requires us to have a viewBox that gets updated with size changes.
        var isEdge = /Edge\/\d./i.test(navigator.userAgent);
        if (!isEdge) {
          svg.removeAttribute("viewBox");
        }
        var update_for_width_change = function() {
            if (isEdge) {
                svg.attributes.viewBox.value = "0 0 " + svg.width.baseVal.value + " " + svg.height.baseVal.value;
            }

            // Keep consistent padding on left and right of frames container.
            frames.attributes.width.value = svg.width.baseVal.value - xpad * 2;

            // Text truncation needs to be adjusted for the current width.
            var el = frames.children;
            for(var i = 0; i < el.length; i++) {
                update_text(el[i]);
            }

            // Keep search elements at a fixed distance from right edge.
            var svgWidth = svg.width.baseVal.value;
            searchbtn.attributes.x.value = svgWidth - xpad - 100;
            matchedtxt.attributes.x.value = svgWidth - xpad - 100;
        };
        window.addEventListener('resize', function() {
            update_for_width_change();
        });
        // This needs to be done asynchronously for Safari to work.
        setTimeout(function() {
            unzoom();
            update_for_width_change();
            restore_state();
        }, 0);
    } else {
        restore_state();
    }
}
// event listeners
window.addEventListener("click", function(e) {
    var target = find_group(e.target);
    if (target) {
        if (target.nodeName == "a") {
            if (e.ctrlKey === false) return;
            e.preventDefault();
        }
        if (target.classList.contains("parent")) unzoom();
        zoom(target);

        // set parameters for zoom state
        var el = target.querySelector("rect");
        if (el && el.attributes && el.attributes.y && el.attributes._orig_x) {
            var params = get_params()
            params.x = el.attributes._orig_x.value;
            params.y = el.attributes.y.value;
            history.replaceState(null, null, parse_params(params));
        }
    }
    else if (e.target.id == "unzoom") {
        unzoom();

        // remove zoom state
        var params = get_params();
        if (params.x) delete params.x;
        if (params.y) delete params.y;
        history.replaceState(null, null, parse_params(params));
    }
    else if (e.target.id == "search") search_prompt();
}, false)
// mouse-over for info
// show
window.addEventListener("mouseover", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = nametype + " " + g_to_text(target);
}, false)
// clear
window.addEventListener("mouseout", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = ' ';
}, false)
// ctrl-F for search
window.addEventListener("keydown",function (e) {
    if (e.keyCode === 114 || (e.ctrlKey && e.keyCode === 70)) {
        e.preventDefault();
        search_prompt();
    }
}, false)
// functions
function get_params() {
    var params = {};
    var paramsarr = window.location.search.substr(1).split('&');
    for (var i = 0; i < paramsarr.length; ++i) {
        var tmp = paramsarr[i].split("=");
        if (!tmp[0] || !tmp[1]) continue;
        params[tmp[0]]  = decodeURIComponent(tmp[1]);
    }
    return params;
}
function parse_params(params) {
    var uri = "?";
    for (var key in params) {
        uri += key + '=' + encodeURIComponent(params[key]) + '&';
    }
    if (uri.slice(-1) == "&")
        uri = uri.substring(0, uri.length - 1);
    if (uri == '?')
        uri = window.location.href.split('?')[0];
    return uri;
}
function find_child(node, selector) {
    var children = node.querySelectorAll(selector);
    if (children.length) return children[0];
    return;
}
function find_group(node) {
    var parent = node.parentElement;
    if (!parent) return;
    if (parent.id == "frames") return node;
    return find_group(parent);
}
function orig_save(e, attr, val) {
    if (e.attributes["_orig_" + attr] != undefined) return;
    if (e.attributes[attr] == undefined) return;
    if (val == undefined) val = e.attributes[attr].value;
    e.setAttribute("_orig_" + attr, val);
}
function orig_load(e, attr) {
    if (e.attributes["_orig_"+attr] == undefined) return;
    e.attributes[attr].value = e.attributes["_orig_" + attr].value;
    e.removeAttribute("_orig_" + attr);
}
function g_to_text(e) {
    var text = find_child(e, "title").firstChild.nodeValue;
    return (text)
}
function g_to_func(e) {
    var func = g_to_text(e);
    // if there's any manipulation we want to do to the function
    // name before it's searched, do it here before returning.
    return (func);
}
function update_text(e) {
    var r = find_child(e, "rect");
    var t = find_child(e, "text");
    var w = parseFloat(r.attributes.width.value) * frames.attributes.width.value / 100 - 3;
    var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
    t.attributes.x.value = format_percent((parseFloat(r.attributes.x.value) + (100 * 3 / frames.attributes.width.value)));
    // Smaller than this size won't fit anything
    if (w < 2 * fontsize * fontwidth) {
        t.textContent = "";
        return;
    }
    t.textContent = txt;
    // Fit in full text width
    if (/^ *\$/.test(txt) || t.getComputedTextLength() < w)
        return;
    if (truncate_text_right) {
        // Truncate the right side of the text.
        for (var x = txt.length - 2; x > 0; x--) {
            if (t.getSubStringLength(0, x + 2) <= w) {
                t.textContent = txt.substring(0, x) + "..";
                return;
            }
        }
    } else {
        // Truncate the left side of the text.
        for (var x = 2; x < txt.length; x++) {
            if (t.getSubStringLength(x - 2, txt.length) <= w) {
                t.textContent = ".." + txt.substring(x, txt.length);
                return;
            }
        }
    }
    t.textContent = "";
}
// zoom
function zoom_reset(e) {
    if (e.attributes != undefined) {
        orig_load(e, "x");
        orig_load(e, "width");
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_reset(c[i]);
    }
}
function zoom_child(e, x, ratio) {
    if (e.attributes != undefined) {
        if (e.attributes.x != undefined) {
            orig_save(e, "x");
            e.attributes.x.value = format_percent((parseFloat(e.attributes.x.value) - x) * ratio);
            if (e.tagName == "text") {
                e.attributes.x.value = format_percent(parseFloat(find_child(e.parentNode, "rect[x]").attributes.x.value) + (100 * 3 / frames.attributes.width.value));
            }
        }
        if (e.attributes.width != undefined) {
            orig_save(e, "width");
            e.attributes.width.value = format_percent(parseFloat(e.attributes.width.value) * ratio);
        }
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_child(c[i], x, ratio);
    }
}
function zoom_parent(e) {
    if (e.attributes) {
        if (e.attributes.x != undefined) {
            orig_save(e, "x");
            e.attributes.x.value = "0.0%";
        }
        if (e.attributes.width != undefined) {
            orig_save(e, "width");
            e.attributes.width.value = "100.0%";
        }
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_parent(c[i]);
    }
}
function zoom(node) {
    var attr = find_child(node, "rect").attributes;
    var width = parseFloat(attr.width.value);
    var xmin = parseFloat(attr.x.value);
    var xmax = xmin + width;
    var ymin = parseFloat(attr.y.value);
    var ratio = 100 / width;
    // XXX: Workaround for JavaScript float issues (fix me)
    var fudge = 0.001;
    unzoombtn.classList.remove("hide");
    var el = frames.children;
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        var a = find_child(e, "rect").attributes;
        var ex = parseFloat(a.x.value);
        var ew = parseFloat(a.width.value);
        // Is it an ancestor
        if (!inverted) {
            var upstack = parseFloat(a.y.value) > ymin;
        } else {
            var upstack = parseFloat(a.y.value) < ymin;
        }
        if (upstack) {
            // Direct ancestor
            if (ex <= xmin && (ex+ew+fudge) >= xmax) {
                e.classList.add("parent");
                zoom_parent(e);
                update_text(e);
            }
            // not in current path
            else
                e.classList.add("hide");
        }
        // Children maybe
        else {
            // no common path
            if (ex < xmin || ex + fudge >= xmax) {
                e.classList.add("hide");
            }
            else {
                zoom_child(e, xmin, ratio);
                update_text(e);
            }
        }
    }
}
function unzoom() {
    unzoombtn.classList.add("hide");
    var el = frames.children;
    for(var i = 0; i < el.length; i++) {
        el[i].classList.remove("parent");
        el[i].classList.remove("hide");
        zoom_reset(el[i]);
        update_text(el[i]);
    }
}
// search
function reset_search() {
    var el = document.querySelectorAll("#frames rect");
    for (var i = 0; i < el.length; i++) {
        orig_load(el[i], "fill")
    }
    var params = get_params();
    delete params.s;
    history.replaceState(null, null, parse_params(params));
}
function search_prompt() {
    if (!searching) {
        var term = prompt("Enter a search term (regexp " +
            "allowed, eg: ^ext4_)", "");
        if (term != null) {
            search(term)
        }
    } else {
        reset_search();
        searching = 0;
        searchbtn.classList.remove("show");
        searchbtn.firstChild.nodeValue = "Search"
        matchedtxt.classList.add("hide");
        matchedtxt.firstChild.nodeValue = ""
    }
}
function search(term) {
    var re = new RegExp(term);
    var el = frames.children;
    var matches = new Object();
    var maxwidth = 0;
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        var func = g_to_func(e);
        var rect = find_child(e, "rect");
        if (func == null || rect == null)
            continue;
        // Save max width. Only works as we have a root frame
        var w = parseFloat(rect.attributes.width.value);
        if (w > maxwidth)
            maxwidth = w;
        if (func.match(re)) {
            // highlight
            var x = parseFloat(rect.attributes.x.value);
            orig_save(rect, "fill");
            rect.attributes.fill.value = searchcolor;
            // remember matches
            if (matches[x] == undefined) {
                matches[x] = w;
            } else {
                if (w > matches[x]) {
                    // overwrite with parent
                    matches[x] = w;
                }
            }
            searching = 1;
        }
    }
    if (!searching)
        return;
    var params = get_params();
    params.s = term;
    history.replaceState(null, null, parse_params(params));

    searchbtn.classList.add("show");
    searchbtn.firstChild.nodeValue = "Reset Search";
    // calculate percent matched, excluding vertical overlap
    var count = 0;
    var lastx = -1;
    var lastw = 0;
    var keys = Array();
    for (k in matches) {
        if (matches.hasOwnProperty(k))
            keys.push(k);
    }
    // sort the matched frames by their x location
    // ascending, then width descending
    keys.sort(function(a, b){
        return a - b;
    });
    // Step through frames saving only the biggest bottom-up frames
    // thanks to the sort order. This relies on the tree property
    // where children are always smaller than their parents.
    var fudge = 0.0001;    // JavaScript floating point
    for (var k in keys) {
        var x = parseFloat(keys[k]);
        var w = matches[keys[k]];
        if (x >= lastx + lastw - fudge) {
            count += w;
            lastx = x;
            lastw = w;
        }
    }
    // display matched percent
    matchedtxt.classList.remove("hide");
    var pct = 100 * count / maxwidth;
    if (pct != 100) pct = pct.toFixed(1);
    matchedtxt.firstChild.nodeValue = "Matched: " + pct + "%";
}
function format_percent(n) {
    return n.toFixed(4) + "%";
}
]]></script><rect x="0" y="0" width="100%" height="634" fill="url(#background)"/><text id="title" x="50.0000%" y="24.00">py-spy record -o profile-rootmaps.svg --pid 3798</text><text id="details" x="10" y="40.00"> </text><text id="unzoom" class="hide" x="10" y="24.00">Reset Zoom</text><text id="search" x="1090" y="24.00">Search</text><text id="matched" x="1090" y="623.00"> </text><svg id="frames" x="10" width="1180"><g><title>run (ipykernel/heartbeat.py:100) (115 samples, 0.28%)</title><rect x="0.0000%" y="100" width="0.2769%" height="15" fill="rgb(227,0,7)"/><text x="0.2500%" y="110.50"></text></g><g><title>_bootstrap (threading.py:890) (164 samples, 0.39%)</title><rect x="0.0000%" y="68" width="0.3949%" height="15" fill="rgb(217,0,24)"/><text x="0.2500%" y="78.50"></text></g><g><title>_bootstrap_inner (threading.py:926) (164 samples, 0.39%)</title><rect x="0.0000%" y="84" width="0.3949%" height="15" fill="rgb(221,193,54)"/><text x="0.2500%" y="94.50"></text></g><g><title>_select_from (pathlib.py:536) (52 samples, 0.13%)</title><rect x="0.6502%" y="596" width="0.1252%" height="15" fill="rgb(248,212,6)"/><text x="0.9002%" y="606.50"></text></g><g><title>_select_from (pathlib.py:573) (116 samples, 0.28%)</title><rect x="0.5563%" y="580" width="0.2793%" height="15" fill="rgb(208,68,35)"/><text x="0.8063%" y="590.50"></text></g><g><title>__hash__ (pathlib.py:755) (56 samples, 0.13%)</title><rect x="0.9440%" y="596" width="0.1349%" height="15" fill="rgb(232,128,0)"/><text x="1.1940%" y="606.50"></text></g><g><title>_select_from (pathlib.py:574) (104 samples, 0.25%)</title><rect x="0.8356%" y="580" width="0.2504%" height="15" fill="rgb(207,160,47)"/><text x="1.0856%" y="590.50"></text></g><g><title>rglob (pathlib.py:1132) (239 samples, 0.58%)</title><rect x="0.5442%" y="564" width="0.5755%" height="15" fill="rgb(228,23,34)"/><text x="0.7942%" y="574.50"></text></g><g><title>stat (pathlib.py:1178) (4,830 samples, 11.63%)</title><rect x="1.2330%" y="580" width="11.6313%" height="15" fill="rgb(218,30,26)"/><text x="1.4830%" y="590.50">stat (pathlib.py:..</text></g><g><title>slugify (memery/loader.py:10) (4,896 samples, 11.79%)</title><rect x="1.1198%" y="564" width="11.7902%" height="15" fill="rgb(220,122,19)"/><text x="1.3698%" y="574.50">slugify (memery/l..</text></g><g><title>indexFlow (memery/core.py:20) (5,183 samples, 12.48%)</title><rect x="0.5033%" y="516" width="12.4813%" height="15" fill="rgb(250,228,42)"/><text x="0.7533%" y="526.50">indexFlow (memery/c..</text></g><g><title>get_image_files (memery/loader.py:14) (5,183 samples, 12.48%)</title><rect x="0.5033%" y="532" width="12.4813%" height="15" fill="rgb(240,193,28)"/><text x="0.7533%" y="542.50">get_image_files (me..</text></g><g><title>&lt;listcomp&gt; (memery/loader.py:14) (5,183 samples, 12.48%)</title><rect x="0.5033%" y="548" width="12.4813%" height="15" fill="rgb(216,20,37)"/><text x="0.7533%" y="558.50">&lt;listcomp&gt; (memery/..</text></g><g><title>__iter__ (tqdm/std.py:1133) (42 samples, 0.10%)</title><rect x="13.5843%" y="548" width="0.1011%" height="15" fill="rgb(206,188,39)"/><text x="13.8343%" y="558.50"></text></g><g><title>image_encoder (memery/encoder.py:17) (291 samples, 0.70%)</title><rect x="13.0087%" y="532" width="0.7008%" height="15" fill="rgb(217,207,13)"/><text x="13.2587%" y="542.50"></text></g><g><title>image_encoder (memery/encoder.py:18) (4,889 samples, 11.77%)</title><rect x="13.7095%" y="532" width="11.7733%" height="15" fill="rgb(231,73,38)"/><text x="13.9595%" y="542.50">image_encoder (me..</text></g><g><title>indexFlow (memery/core.py:30) (5,190 samples, 12.50%)</title><rect x="13.0087%" y="516" width="12.4982%" height="15" fill="rgb(225,20,46)"/><text x="13.2587%" y="526.50">indexFlow (memery/c..</text></g><g><title>indexFlow (memery/core.py:34) (30,078 samples, 72.43%)</title><rect x="25.5478%" y="516" width="72.4317%" height="15" fill="rgb(210,31,41)"/><text x="25.7978%" y="526.50">indexFlow (memery/core.py:34)</text></g><g><title>build_treemap (memery/indexer.py:25) (30,077 samples, 72.43%)</title><rect x="25.5503%" y="532" width="72.4293%" height="15" fill="rgb(221,200,47)"/><text x="25.8003%" y="542.50">build_treemap (memery/indexer.py:25)</text></g><g><title>_save (torch/serialization.py:476) (108 samples, 0.26%)</title><rect x="98.1120%" y="564" width="0.2601%" height="15" fill="rgb(226,26,5)"/><text x="98.3620%" y="574.50"></text></g><g><title>_save (torch/serialization.py:491) (500 samples, 1.20%)</title><rect x="98.3769%" y="564" width="1.2041%" height="15" fill="rgb(249,33,26)"/><text x="98.6269%" y="574.50"></text></g><g><title>save_archives (memery/indexer.py:42) (680 samples, 1.64%)</title><rect x="97.9796%" y="532" width="1.6375%" height="15" fill="rgb(235,183,28)"/><text x="98.2296%" y="542.50"></text></g><g><title>save (torch/serialization.py:372) (680 samples, 1.64%)</title><rect x="97.9796%" y="548" width="1.6375%" height="15" fill="rgb(221,5,38)"/><text x="98.2296%" y="558.50"></text></g><g><title>queryFlow (memery/core.py:53) (41,202 samples, 99.22%)</title><rect x="0.3997%" y="500" width="99.2198%" height="15" fill="rgb(247,18,42)"/><text x="0.6497%" y="510.50">queryFlow (memery/core.py:53)</text></g><g><title>indexFlow (memery/core.py:37) (681 samples, 1.64%)</title><rect x="97.9796%" y="516" width="1.6399%" height="15" fill="rgb(241,131,45)"/><text x="98.2296%" y="526.50"></text></g><g><title>_rebuild_tensor (torch/_utils.py:132) (48 samples, 0.12%)</title><rect x="99.7809%" y="580" width="0.1156%" height="15" fill="rgb(249,31,29)"/><text x="100.0309%" y="590.50"></text></g><g><title>_rebuild_tensor_v2 (torch/_utils.py:137) (63 samples, 0.15%)</title><rect x="99.7760%" y="564" width="0.1517%" height="15" fill="rgb(225,111,53)"/><text x="100.0260%" y="574.50"></text></g><g><title>queryFlow (memery/core.py:55) (142 samples, 0.34%)</title><rect x="99.6195%" y="500" width="0.3420%" height="15" fill="rgb(238,160,17)"/><text x="99.8695%" y="510.50"></text></g><g><title>db_loader (memery/loader.py:42) (142 samples, 0.34%)</title><rect x="99.6195%" y="516" width="0.3420%" height="15" fill="rgb(214,148,48)"/><text x="99.8695%" y="526.50"></text></g><g><title>load (torch/serialization.py:594) (142 samples, 0.34%)</title><rect x="99.6195%" y="532" width="0.3420%" height="15" fill="rgb(232,36,49)"/><text x="99.8695%" y="542.50"></text></g><g><title>_load (torch/serialization.py:853) (141 samples, 0.34%)</title><rect x="99.6219%" y="548" width="0.3395%" height="15" fill="rgb(209,103,24)"/><text x="99.8719%" y="558.50"></text></g><g><title>wrapper (tornado/gen.py:234) (41,361 samples, 99.60%)</title><rect x="0.3949%" y="292" width="99.6027%" height="15" fill="rgb(229,88,8)"/><text x="0.6449%" y="302.50">wrapper (tornado/gen.py:234)</text></g><g><title>dispatch_shell (ipykernel/kernelbase.py:268) (41,361 samples, 99.60%)</title><rect x="0.3949%" y="308" width="99.6027%" height="15" fill="rgb(213,181,19)"/><text x="0.6449%" y="318.50">dispatch_shell (ipykernel/kernelbase.py:268)</text></g><g><title>comm_msg (ipykernel/comm/manager.py:111) (41,361 samples, 99.60%)</title><rect x="0.3949%" y="324" width="99.6027%" height="15" fill="rgb(254,191,54)"/><text x="0.6449%" y="334.50">comm_msg (ipykernel/comm/manager.py:111)</text></g><g><title>handle_msg (ipykernel/comm/comm.py:161) (41,361 samples, 99.60%)</title><rect x="0.3949%" y="340" width="99.6027%" height="15" fill="rgb(241,83,37)"/><text x="0.6449%" y="350.50">handle_msg (ipykernel/comm/comm.py:161)</text></g><g><title>m (ipywidgets/widgets/widget.py:217) (41,361 samples, 99.60%)</title><rect x="0.3949%" y="356" width="99.6027%" height="15" fill="rgb(233,36,39)"/><text x="0.6449%" y="366.50">m (ipywidgets/widgets/widget.py:217)</text></g><g><title>_handle_msg (ipywidgets/widgets/widget.py:685) (41,360 samples, 99.60%)</title><rect x="0.3973%" y="372" width="99.6003%" height="15" fill="rgb(226,3,54)"/><text x="0.6473%" y="382.50">_handle_msg (ipywidgets/widgets/widget.py:685)</text></g><g><title>_handle_custom_msg (ipywidgets/widgets/widget.py:693) (41,360 samples, 99.60%)</title><rect x="0.3973%" y="388" width="99.6003%" height="15" fill="rgb(245,192,40)"/><text x="0.6473%" y="398.50">_handle_custom_msg (ipywidgets/widgets/widget.py:693)</text></g><g><title>__call__ (ipywidgets/widgets/widget.py:186) (41,360 samples, 99.60%)</title><rect x="0.3973%" y="404" width="99.6003%" height="15" fill="rgb(238,167,29)"/><text x="0.6473%" y="414.50">__call__ (ipywidgets/widgets/widget.py:186)</text></g><g><title>_handle_button_msg (ipywidgets/widgets/widget_button.py:105) (41,360 samples, 99.60%)</title><rect x="0.3973%" y="420" width="99.6003%" height="15" fill="rgb(232,182,51)"/><text x="0.6473%" y="430.50">_handle_button_msg (ipywidgets/widgets/widget_button.py:105)</text></g><g><title>click (ipywidgets/widgets/widget_button.py:94) (41,360 samples, 99.60%)</title><rect x="0.3973%" y="436" width="99.6003%" height="15" fill="rgb(231,60,39)"/><text x="0.6473%" y="446.50">click (ipywidgets/widgets/widget_button.py:94)</text></g><g><title>__call__ (ipywidgets/widgets/widget.py:186) (41,360 samples, 99.60%)</title><rect x="0.3973%" y="452" width="99.6003%" height="15" fill="rgb(208,69,12)"/><text x="0.6473%" y="462.50">__call__ (ipywidgets/widgets/widget.py:186)</text></g><g><title>page_update (memery/gui.py:69) (41,360 samples, 99.60%)</title><rect x="0.3973%" y="468" width="99.6003%" height="15" fill="rgb(235,93,37)"/><text x="0.6473%" y="478.50">page_update (memery/gui.py:69)</text></g><g><title>update_tabs (memery/gui.py:34) (41,360 samples, 99.60%)</title><rect x="0.3973%" y="484" width="99.6003%" height="15" fill="rgb(213,116,39)"/><text x="0.6473%" y="494.50">update_tabs (memery/gui.py:34)</text></g><g><title>all (41,526 samples, 100%)</title><rect x="0.0000%" y="52" width="100.0000%" height="15" fill="rgb(222,207,29)"/><text x="0.2500%" y="62.50"></text></g><g><title>_run_module_as_main (runpy.py:193) (41,362 samples, 99.61%)</title><rect x="0.3949%" y="68" width="99.6051%" height="15" fill="rgb(206,96,30)"/><text x="0.6449%" y="78.50">_run_module_as_main (runpy.py:193)</text></g><g><title>_run_code (runpy.py:85) (41,362 samples, 99.61%)</title><rect x="0.3949%" y="84" width="99.6051%" height="15" fill="rgb(218,138,4)"/><text x="0.6449%" y="94.50">_run_code (runpy.py:85)</text></g><g><title>&lt;module&gt; (ipykernel_launcher.py:16) (41,362 samples, 99.61%)</title><rect x="0.3949%" y="100" width="99.6051%" height="15" fill="rgb(250,191,14)"/><text x="0.6449%" y="110.50">&lt;module&gt; (ipykernel_launcher.py:16)</text></g><g><title>launch_instance (traitlets/config/application.py:845) (41,362 samples, 99.61%)</title><rect x="0.3949%" y="116" width="99.6051%" height="15" fill="rgb(239,60,40)"/><text x="0.6449%" y="126.50">launch_instance (traitlets/config/application.py:845)</text></g><g><title>start (ipykernel/kernelapp.py:612) (41,362 samples, 99.61%)</title><rect x="0.3949%" y="132" width="99.6051%" height="15" fill="rgb(206,27,48)"/><text x="0.6449%" y="142.50">start (ipykernel/kernelapp.py:612)</text></g><g><title>start (tornado/platform/asyncio.py:199) (41,362 samples, 99.61%)</title><rect x="0.3949%" y="148" width="99.6051%" height="15" fill="rgb(225,35,8)"/><text x="0.6449%" y="158.50">start (tornado/platform/asyncio.py:199)</text></g><g><title>run_forever (asyncio/base_events.py:541) (41,362 samples, 99.61%)</title><rect x="0.3949%" y="164" width="99.6051%" height="15" fill="rgb(250,213,24)"/><text x="0.6449%" y="174.50">run_forever (asyncio/base_events.py:541)</text></g><g><title>_run_once (asyncio/base_events.py:1786) (41,362 samples, 99.61%)</title><rect x="0.3949%" y="180" width="99.6051%" height="15" fill="rgb(247,123,22)"/><text x="0.6449%" y="190.50">_run_once (asyncio/base_events.py:1786)</text></g><g><title>_run (asyncio/events.py:88) (41,362 samples, 99.61%)</title><rect x="0.3949%" y="196" width="99.6051%" height="15" fill="rgb(231,138,38)"/><text x="0.6449%" y="206.50">_run (asyncio/events.py:88)</text></g><g><title>&lt;lambda&gt; (tornado/ioloop.py:688) (41,362 samples, 99.61%)</title><rect x="0.3949%" y="212" width="99.6051%" height="15" fill="rgb(231,145,46)"/><text x="0.6449%" y="222.50">&lt;lambda&gt; (tornado/ioloop.py:688)</text></g><g><title>_run_callback (tornado/ioloop.py:741) (41,362 samples, 99.61%)</title><rect x="0.3949%" y="228" width="99.6051%" height="15" fill="rgb(251,118,11)"/><text x="0.6449%" y="238.50">_run_callback (tornado/ioloop.py:741)</text></g><g><title>inner (tornado/gen.py:814) (41,362 samples, 99.61%)</title><rect x="0.3949%" y="244" width="99.6051%" height="15" fill="rgb(217,147,25)"/><text x="0.6449%" y="254.50">inner (tornado/gen.py:814)</text></g><g><title>run (tornado/gen.py:775) (41,362 samples, 99.61%)</title><rect x="0.3949%" y="260" width="99.6051%" height="15" fill="rgb(247,81,37)"/><text x="0.6449%" y="270.50">run (tornado/gen.py:775)</text></g><g><title>process_one (ipykernel/kernelbase.py:365) (41,362 samples, 99.61%)</title><rect x="0.3949%" y="276" width="99.6051%" height="15" fill="rgb(209,12,38)"/><text x="0.6449%" y="286.50">process_one (ipykernel/kernelbase.py:365)</text></g></svg></svg>